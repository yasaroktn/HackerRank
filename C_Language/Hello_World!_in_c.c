#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
	
    char s[100];
    char a[100];
    char b[100];
    char c[100];
    char d[100];
    int i = 1;
    printf ("Hello, World!\n");
    scanf("%s%s%s%s", &s, &a, &b, &c);
    printf("%s %s %s %s", s, a, b, c);
    return 0;
}

